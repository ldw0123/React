// 주요 기본형 타입: number, string, boolean

let age: number;
age = 12.3;

let userName: string;
userName = 'Asahi';

let isInstructor: boolean;
isInstructor = false;

let hobbies: null; // null 타입으로 지정하는 경우는 거의 없다
// hobbies = 12; // 에러!
